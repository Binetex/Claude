# Floremart — Refactor Backlog

Приоритизированный бэклог, собранный в ночном аудите. Метки риска:
🟢 безопасно/additive · 🟡 требует тестов · 🔴 нужно решение владельца/миграция (НЕ трогать ночью).

Статусы: `[ ]` не начато · `[~]` в работе этой ночью · `[x]` сделано этой ночью.

## A. Интеграционная основа
- [x] 🟢 Нормализованные типы заказа/клиента/адреса/статусов/событий (`integrations/normalized.ts`).
- [x] 🟢 Интерфейсы `WebhookAdapter`, `ConnectionAdapter`, расширение `OrderAdapter`/`DeliveryAdapter`.
- [x] 🟢 Типизированные ошибки интеграций + централизованная retry-политика.
- [x] 🟢 Реестр адаптеров по платформе (order/webhook/connection).
- [x] 🟢 Skeleton-адаптеры Woo/Telegram/Quo + mock clients.
- [x] 🟢 Contract tests для адаптеров (общая сюита совместимости).
- [ ] 🟡 Внедрить `NormalizedOrder` в Shopify ingest как промежуточный слой (сейчас маппинг прямой).
      Отложено: требует аккуратного рефакторинга рабочего ingest с полным regression-покрытием.
- [ ] 🔴 Woo REST реальная реализация (нужны credentials + решение по маппингу статусов).

## B. Event-driven
- [x] 🟢 Типизированный реестр доменных событий + payload-типы (`events/`).
- [x] 🟢 In-process шина с idempotency-ключом, retry-метаданными, журналом, изоляцией хендлеров.
- [x] 🟢 Тесты шины (dedup, изоляция ошибок, retry-метаданные).
- [ ] 🟡 Подключить публикацию событий в реальные потоки (ingest/assignments) за фичефлагом.
      Отложено: чтобы не менять поведение рабочих потоков без полного regression-набора.
- [ ] 🔴 Замена in-process на Redis/BullMQ (инфраструктурное решение + зависимость).

## C. Уведомления
- [x] 🟢 Единый `MessageCommand`/`MessageResult`, шаблоны, классификация retry (`messaging/`).
- [x] 🟢 Mock-провайдеры SMS/email/Telegram/push + тесты.
- [x] 🟢 Подписчик `order.delivery.completed` → фан-аут (за mock, без реальной отправки).
- [ ] 🔴 Реальные провайдеры (Quo/Telegram/SMS/email/push) — нужны credentials и шаблоны.
- [ ] 🟡 Воркер для `lib/jobs.enqueue` (сейчас без потребителя — задачи не исполняются).

## D. UI / дизайн-система
- [x] 🟢 Дизайн-токены (`docs/DESIGN_SYSTEM.md` + CSS-переменные/утилиты).
- [x] 🟢 Общие состояния списков: `EmptyState`, `ErrorState`, `LoadingState`, `Skeleton`.
- [~] 🟡 Orders list / Order details / «Сегодня нужно купить» / мобильные карточки — аккуратный
      рефактор без изменения бизнес-логики и без удаления действий.
- [ ] 🟡 Унифицировать ad-hoc цвета статусов в `lib/statuses.ts` на семантические токены
      (сейчас часть на TONE, часть на произвольных классах). Требует визуальной проверки.
- [ ] ⛔ Products/Sites/Florists/Users/Dashboard — НЕ трогать, пока Orders не завершён и не проверен.

## E. Данные / производительность (read-only анализ)
- [ ] 🟡 Пагинация списков заказов/товаров (`findMany` без limit) — рост числа записей.
- [ ] 🟡 Проверить N+1 в списках (include уже используется — валидировать под нагрузкой).
- [ ] 🔴 Любые изменения схемы (индексы, поля) → `docs/PROPOSED_SCHEMA_CHANGES.md`, не мигрировать ночью.

## F. Тесты
- [x] 🟢 Contract tests адаптеров, тесты событий, тесты messaging, тесты webhook HMAC/replay/idempotency,
      тесты нормализации статусов.
- [ ] 🟡 Расширить покрытие ролевых сериализаторов (owner/callcenter/florist visibility) — частично есть.

## G. Известные несоответствия (для владельца)
- `lib/jobs.enqueue` не имеет обработчика — поставленные задачи только логируются. Реальные
  уведомления/пуш-синк не отправляются, пока нет воркера/подписчиков (см. C).
- Feature-флаги интеграций по умолчанию `false` — адаптеры остаются стабами независимо от флага
  до появления реальных реализаций.
