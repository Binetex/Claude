/**
 * Предел длины инструкции курьеру.
 *
 * Обычный модуль (не "server-only"): значение нужно И серверной проверке в
 * `updateOrderBlock`, И клиентской форме со счётчиком символов. Импорт значения из
 * server-only модуля в клиентский компонент ломает сборку — см. правило в CLAUDE.md.
 */
export const COURIER_NOTE_MAX = 500;
