import { redirect } from "next/navigation";

/**
 * Вход в раздел «Финансы». Собственного содержимого не имеет: пункт меню ведёт сюда,
 * а показывать надо список флористов с балансами — он и есть главный экран раздела.
 *
 * Отдельная страница-перенаправление, а не ссылка сразу на подстраницу в меню: так адрес
 * раздела остаётся стабильным, даже если главный экран однажды сменится.
 */
export default async function FinanceIndexPage() {
  redirect("/dashboard/finance/florists");
}
