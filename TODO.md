# TODO — Floremart

Обновлено **2026-07-21**. Порядок внутри разделов — по приоритету.
Контекст и обоснования — см. [HANDOFF.md](./docs/HANDOFF.md) и [CHANGELOG.md](./CHANGELOG.md).

## 🔴 Блокирует работу прямо сейчас

- [x] Флористы в проде созданы (напр. Анита/Наташа для TheFlow); авто-назначение работает для Shopify и WooCommerce
- [ ] Создать специалиста колл-центра, если нужен доступ без финансов

## 🟠 Функциональные пробелы (админка)

- [ ] UI редактирования приоритета флористов по сайту (сейчас только авто-расстановка при создании нового сайта, ручной правки нет)
- [ ] UI создания/редактирования `Site` вручную (сейчас сайты появляются только через Shopify OAuth-подключение)
- [ ] UI редактирования/деактивации пользователей
- [ ] Смена пароля пользователя (self-service или владельцем)
- [ ] Показ email/логина для входа сразу после создания пользователя в более заметном месте (сейчас только пароль в баннере)

## 🟡 Shopify — доработка существующей интеграции

- [x] `pushUpdate`: запись адреса получателя и открытки (`cardMessage`, через стандартное поле заказа Shopify `note` — у этого магазина открытка всегда там, не в note_attributes) обратно в Shopify (`shipping_address`/`note`) — см. `src/integrations/shopify/pushUpdate.ts`. `customerNote` и дата/интервал доставки **сознательно не пишутся обратно**: `customerNote` — ручное поле владельца/колл-центра, у Shopify нет для него отдельного поля; дата/интервал доставки парсятся из `note_attributes`, которое Shopify не позволяет менять после создания заказа (см. CLAUDE.md §11–12).
- [ ] Синхронизация каталога товаров из Shopify (авто-создание `Product` по `variant_id`/`product_id`, чтобы авто-цена работала без ручной настройки владельцем)
- [ ] Проверить/расширить эвристику извлечения даты доставки из `note_attributes` под реальные поля темы O'hara Florist (сейчас — общий regex, может не совпасть с конкретными названиями полей)
- [x] Обработать `orders/updated` полнее — адрес получателя и `cardMessage` теперь тоже обновляются (см. `applyUpdateFromShopify` в `ingestOrder.ts`), `customerNote`/дата/интервал доставки по-прежнему не трогаются на update
- [ ] Отзыв доступа / отключение магазина (сейчас нет UI для "отключить"/"переустановить" Shopify-подключение)
- [ ] Обработка вебхука `app/uninstalled` (что делать с `Site`, если владелец магазина сам удалит приложение в Shopify)

## 🟡 Надёжность / инфраструктура обработки заказов

- [ ] Реальная фоновая очередь (BullMQ/Redis) вместо синхронной обработки вебхука в HTTP-хендлере
- [ ] Периодический добор пропущенных заказов (на случай, если вебхук Shopify не был доставлен после всех ретраев)
- [ ] Мониторинг/алерты на ошибки обработки вебхука (сейчас только `console.error` в PM2-логи, никто не уведомляется)

## 🟢 Другие интеграции

- [x] **WooCommerce** — реальная реализация (API-ключи, вебхуки, маппинг заказов, классификация оплаты, авто-назначение, Fees→tip, billing-адрес)
- [x] **QUO (ex-OpenPhone)** — SMS/звонки/записи/транскрипты, привязка к заказам, «Нераспознанные», backfill; live для TheFlow
- [x] **Burq** — draft-first создание доставки, POD, retry, per-site dropoff, статусы/времена в UI
- [ ] Telegram — уведомления флористам о назначении, приём/отказ через бота (пока стаб `notifyFloristAssigned`)

## 🔵 Production / DevOps

- [ ] Проверить `pm2 startup`/systemd (`pm2-claudecode.service`) реальной перезагрузкой сервера — сделать **только с явным подтверждением владельца**, сервер общий с другими сайтами
- [ ] Перенести `../floremart-shopify-app` (Shopify CLI-конфиг) под контроль версий
- [ ] Настроить регулярный бэкап production Neon (встроенное branching или `pg_dump` по расписанию — см. DEPLOY.md раздел 6)
- [ ] Рассмотреть ротацию секретов, которые в процессе разработки могли попасть в диалоги/логи (`AUTH_SECRET`, `SHOPIFY_CLIENT_SECRET`, пароль `DATABASE_URL`) — см. HANDOFF.md раздел 14

## ⚪ Мелочи / полировка (не блокируют, можно отложить надолго)

- [ ] Favicon/OG-изображение для публичного лендинга
- [ ] Более подробная информация об ошибке на странице OAuth-callback при сбое (сейчас голый JSON)
- [ ] Логотипы/цветовые метки для подключённых через Shopify сайтов (сейчас дефолтный серый `colorTag`)
