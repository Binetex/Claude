-- Ручная категория переписки в разделе «Другие сообщения».
-- Аддитивно: nullable-колонка, старому коду невидима, бэкфилл не нужен —
-- NULL означает «категорию считает правило по тексту».
ALTER TABLE "OrderCommunication" ADD COLUMN "topicManual" TEXT;
