-- Откуда взялась фактическая стоимость доставки: "MANUAL" (владелец вписал руками) или "BURQ"
-- (вебхук/массовое подтверждение). Обе дороги пишут одно поле и одну отметку подтверждения,
-- поэтому без признака источник не восстановить. NULL = старые записи, источник неизвестен.
ALTER TABLE "Order" ADD COLUMN "deliveryActualCostSource" TEXT;
