-- Тексты заготовок ассистента на частые вопросы («где заказ», «во сколько привезут»).
-- JSON, а не таблица: заготовок горстка, у каждой два поля, отдельная сущность здесь ничего не
-- добавляет. Пусто — работают тексты по умолчанию из кода. Только добавление.
ALTER TABLE "Site" ADD COLUMN "aiTemplatesJson" JSONB;
