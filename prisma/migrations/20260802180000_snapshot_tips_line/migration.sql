-- Чаевые отдельной строкой в снимке расчёта.
--
-- Только добавление колонки. Существующие ревизии не изменяются: у них tipsCents = 0,
-- а grossRevenueCents не включает чаевые — такая ревизия остаётся внутренне
-- непротиворечивой, её сумма строк по-прежнему сходится с распределяемой прибылью.
--
-- Смысл разделения — представление, а не арифметика: верхняя строка расчёта теперь
-- совпадает с суммой оплаченных заказов на платформе, а чаевые видно как вычет, а не
-- как молча выброшенную величину. Распределяемая прибыль и начисления не меняются.

ALTER TABLE "OrderFinancialSnapshot" ADD COLUMN "tipsCents" INTEGER NOT NULL DEFAULT 0;

ALTER TABLE "OrderFinancialSnapshot"
    ADD CONSTRAINT "OFS_tips_non_negative" CHECK ("tipsCents" >= 0);
