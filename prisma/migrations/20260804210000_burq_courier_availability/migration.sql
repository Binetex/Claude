-- Проверка наличия курьеров на маршрут заказа.
--
-- Сразу после создания Burq-черновика спрашиваем котировки по маршруту «точка забора →
-- адрес доставки». Пустой список котировок = маршрут никто не берётся везти; именно про этот
-- случай владельцу и флористу показывается уведомление.
--
-- Обе колонки NULLABLE и без DEFAULT: NULL = проверка не выполнялась, и это нормальное
-- состояние всех существующих доставок. Backfill не нужен.

ALTER TABLE "Delivery" ADD COLUMN "couriersCheckedAt" TIMESTAMP(3);
ALTER TABLE "Delivery" ADD COLUMN "couriersAvailable" INTEGER;
