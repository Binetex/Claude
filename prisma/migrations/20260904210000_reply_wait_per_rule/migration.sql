-- Срок ожидания на КАЖДОМ правиле, а не только на магазине.
--
-- Двух чисел на магазин («первое ожидание» и «все следующие») хватало ровно на двухшаговую
-- лесенку. На четырёх шагах второй, третий и четвёртый обязаны ждать одинаково, а по смыслу
-- у них паузы разные: вопрос — час, напоминание — день, последнее слово заказчику — до завтра.
--
-- Только добавление: пусто = как раньше, срок берётся из настроек магазина. Существующие
-- правила не трогаем, поведение не меняется.
ALTER TABLE "Automation" ADD COLUMN "noReplyAfterMin" INTEGER;
