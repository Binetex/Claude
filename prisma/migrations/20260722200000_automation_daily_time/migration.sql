-- Время ежедневных триггеров автоматизаций в локальном времени магазина ("HH:mm").
-- Используется триггером «Доставка сегодня»: задача публикуется отложенно на это время
-- локального дня доставки. По умолчанию 09:00 — существующие магазины получают его сразу.
ALTER TABLE "Site" ADD COLUMN "automationDailyLocalTime" TEXT NOT NULL DEFAULT '09:00';
