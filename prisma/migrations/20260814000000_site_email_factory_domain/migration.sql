-- Домен Email Factory для ручной переписки магазина. Отдельно от SiteEmailSettings (там Brevo):
-- один адрес на два независимых канала означал бы, что включение рассылок молча меняет обратный
-- адрес переписки. NULL = не выбран; при единственном домене в аккаунте он берётся и без выбора.
ALTER TABLE "Site" ADD COLUMN "emailFactoryDomain" TEXT;
